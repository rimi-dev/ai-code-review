apiVersion: v1
kind: Service
metadata:
  name: review-bot
  namespace: ai-code-review
  labels:
    app: review-bot
    part-of: ai-code-review
spec:
  type: ClusterIP
  selector:
    app: review-bot
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
